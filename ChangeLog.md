# ChangeLog

本文件记录 VideoGenFlow 的重要功能、交互与修复。日期采用 `YYYY-MM-DD`。

## [Unreleased] - 2026-08-09

### Seedance 视频生成工作流

- 新增“图片成片 / 视频成片”选择：图片成片沿用静态分镜合成，视频成片先将选中的分镜图动态化再进行最终合成。
- 接入 Seedance 2.0 mini 首帧图生视频，使用分镜图作为首帧、分镜已有 `video_prompt` 作为提示词，固定生成 480p 无声视频。
- 视频生成时长参考对应分镜时长，并限制在模型支持的 4–15 秒区间。
- 支持智能、全部和自定义镜头三种策略；智能策略固定保留首尾镜头，并根据动作强度、运镜信息和镜头间隔控制生成数量。
- 新增分镜视频资产模型、仓储、API、后台任务、方舟异步任务轮询、本地 MP4 缓存及静态资源访问路由。
- 已完成且提示词未变化的分镜视频可复用；生成失败或未选择的镜头在最终成片中自动回退为静态图片。
- 最终 ffmpeg 时间线支持动态视频与静态分镜混合，动态片段按音频时间轴循环或裁切，并移除原视频音轨。

### 成本与任务可靠性

- 预算由“按条估算”改为按生成时长计费，默认 `ARK_VIDEO_COST_PER_SECOND=0.25`。
- 预计费用按所选镜头生成秒数之和 × 每秒单价计算，仅用于提交前预算提示，不代表供应商最终账单。
- 付费视频任务必须经过用户明确确认后才提交。
- 修复开发热重载或服务重启导致远端任务成功、本地状态仍停留在“生成中”的问题。
- 服务启动时自动恢复 `pending/generating` 任务；资产已有方舟 `task_id` 时只恢复轮询和下载，不重复创建付费任务。
- 防止同一镜头生成期间因重复操作产生重复任务和重复费用。

### 前端交互

- 新增视频成片方式与生成策略选择弹窗，展示所选镜头、预计数量和预计费用。
- 分镜视频在会话正文中按照分镜图画廊形式平铺展示。
- 每个视频卡片展示镜号、时长、清晰度、费用以及排队、生成、完成、失败状态；完成后支持卡片内直接播放。
- 会话切换后继续轮询服务端状态，恢复正在生成的分镜视频进度。
- 修复“重新合成”按钮将 React 点击事件误传为 `render_mode`，导致循环结构无法 JSON 序列化的问题。
- 重新合成会明确沿用当前的图片成片或视频成片模式。

### 合成与字幕修复

- 修复长视频字幕在约 12 秒后消失：字幕 PNG 输入改为持续帧流，确保整段口播时间轴均可烧录字幕。
- 混合成片中的静态分镜新增线性 Ken Burns 缩放，相邻镜头交替缓慢推近和拉远，幅度约 8%。
- 缩放步长根据每个镜头的实际时长计算，保证不同长度镜头均匀完成；动态视频镜头保持原始运动，不做二次缩放。

### 验证

- 使用会话 `1af8ba5e-bb49-425c-b8f8-ad5d597bad0c` 完成真实 Seedance 首帧图生视频验证：480p、5 秒、H.264，任务成功并落地本地文件。
- 验证 5 秒视频预算为 `5 × 0.25 = ¥1.25`。
- 基于动态与静态镜头重新合成 65.568 秒成片，字幕抽查覆盖 2、11、20、50、64 秒时间点。
- 验证静态镜头线性缩放、动态镜头复用、配音、背景音乐和字幕可共同完成合成。
- 后端通过 `compileall`，前端通过 Next.js 生产构建、Lint 和 TypeScript 类型检查。

## [Unreleased] - 2026-08-08

### Agent 与对话交互

- 将客户端从阶段表单式交互改造成对话式 Creative Agent。
- 新会话支持主动欢迎、闲聊和创作主线引导。
- 支持通过短视频链接、话题和现成口播文案启动创作。
- 增加阶段确认卡片，每一步完成后主动询问修改或继续。
- 增加脚本、分镜、出图、配音和成片的持续进度提示。
- 使用 SSE 流式输出 Agent 文本、节点开始/结束和执行状态。
- 修复切换会话后正在执行状态消失的问题，状态由服务端任务记录恢复。
- 确认按钮在处理中置灰并展示 loading，避免重复触发。
- 修复“查看成片”错误触发“正在启动创作任务”的问题。
- 修复脚本生成完成后侧栏标题必须刷新才更新的问题。
- 会话标题默认使用最终口播脚本标题。
- 优化脚本、分析、分镜和下一步卡片的信息层级。

### 意图识别与上下文决策

- 新增制作意图 `generate_tts` 和 `render_video`。
- 为“重新语音合成”“语音换成豆包”“重新合成成片”等指令增加确定性优先路由。
- 修复“语音部分换成豆包再试试”被识别为修改脚本的问题。
- 分类提示词明确规定语音、配音、声音和 TTS 供应商调整不得判为脚本修改。
- 对话触发 TTS 时可从用户原话解析豆包或 DubbingX 供应商。
- 加载上下文时同时读取当前脚本、分镜、音轨和成片。
- 增加依赖感知工作流，识别脚本修改后旧分镜、旧音轨和旧成片的失效状态。
- 增加执行前素材规划：重新合成前检查当前与历史分镜的图片完整性。
- 当前分镜不完整时，按版本从新到旧寻找可复用的完整分镜。
- 增加历史分镜与当前口播的匹配度门槛：高匹配自动复用，中等匹配需要明确授权，低于 45% 拒绝强行复用。
- Agent 回复展示复用的分镜版本、镜头数、匹配度和对齐策略。

### 脚本与分镜质量

- 优化参考视频分析、同款仿写与普通脚本生成提示词。
- 强化原视频钩子、节奏、冲突、心理机制和金句结构的抽取与迁移。
- 改进脚本局部修改规则，未点名部分尽量保持不变。
- 优化分镜提示词的镜头差异要求，减少连续镜头画面高度重复。
- 改进人物、场景、景别、机位、动作和视觉隐喻的变化约束。
- 脚本和分镜保留版本历史、激活与回退能力。

### 分镜图与预览

- 接入火山方舟 Seedream 图片生成。
- 使用首镜文生图、后续镜头参考前一镜图生图的链式生成方式。
- 支持单镜重绘和整批重新生成。
- 分镜图点击后支持大图预览。
- 大图预览支持上一张、下一张按钮和键盘方向键切换。
- 修复会话切换后图片生成状态不能恢复的问题。

### TTS 与字幕

- 新增完整音轨模型、仓储、API、后台任务和前端状态展示。
- 支持 DubbingX 和豆包/火山引擎两种 TTS 供应商。
- 前端设置中增加供应商选择；自定义音色和参数暂不开放。
- 豆包默认音色设为“爽快思思”：`zh_female_shuangkuaisisi_emo_v2_mars_bigtts`。
- 豆包默认情感设为 `coldness`，音调 -1，音量默认。
- 官方体验页“语速 20”映射为 HTTP API `speed_ratio=1.2`。
- 修复将“语速 20”错误映射为 `speed_ratio=2.0` 导致接近双倍速的问题。
- 修复豆包经典 HTTP TTS 返回业务码 `3000 / Success` 却被客户端判定失败的问题。
- 增加成功响应缺少音频数据的错误检查。
- 接入火山引擎 ATA，生成逐句和逐词字幕时间戳。
- 新音轨创建后立即通过 SSE 推送给前端，消除状态空窗。
- 增加 TTS 失败状态和重新生成入口。

### 音画同步与成片

- 新增成片模型、仓储、API、后台任务和前端播放入口。
- 使用 ffmpeg 将静态分镜、TTS 配音、字幕和背景音乐合成为 MP4。
- 使用音频时长和 ATA 时间轴作为唯一主时钟。
- 将“按总字数比例切镜头”升级为基于文本匹配锚点的单调语义对齐。
- 历史分镜与新脚本存在局部增删时，在相邻语义锚点间局部插值，避免后续镜头累计偏移。
- 新脚本已删除的旧镜头会被压缩跳过。
- 成片记录固定引用具体音轨和分镜版本，保证任务执行期间素材快照稳定。
- 字幕改为短视频常见亮黄色、粗黑描边样式。
- 烧录字幕前清理容易产生异常垂直字框的停顿标点。
- 新增 `bg_music/` 背景音乐目录，合成时自动选择音乐并循环。
- 调整背景音乐混音，使其可感知但不覆盖口播。
- 分镜、音轨或图片缺失时给出具体缺失数量和下一步建议。
- 新增“图片成片 / 视频成片”可选工作流。
- 接入 Seedance 2.0 mini 首帧图生视频，固定 480p、无声，时长参考对应分镜并限制在模型支持区间。
- 直接复用分镜脚本已有的 `video_prompt`，无需重复生成提示词。
- 视频成片支持智能、全部和自定义镜头三种生成策略。
- 智能策略固定选择首镜、尾镜，并按动作强度、运镜信息和镜头间隔控制生成数量。
- 付费任务提交前展示预计视频数量与费用并要求明确确认。
- 新增分镜视频资产状态、任务 ID、本地缓存、错误信息和费用记录。
- 最终 ffmpeg 时间线支持动态视频与静态图片混合；视频失败镜头自动回退图片。

### 前端状态与数据刷新

- 新增 `WorkflowDock`，根据真实产物依赖展示当前阶段和下一步动作。
- TTS 和成片产物支持轮询，并在 SSE artifact 事件到达时立即写入查询缓存。
- 消息保存和任务结束时统一刷新脚本、分镜、图片、分析、音轨和成片查询。
- 修复生成新配音后仍展示旧成片为当前成片的问题。
- 新配音完成后主动引导重新合成成片。
- 新成片使用最新音轨但复用历史画面时，仍能正确显示为已完成。

### 平台与工程能力

- 后端升级为 FastAPI + LangGraph + SQLAlchemy 分层结构。
- 增加 JWT 注册、登录、当前用户和账户删除能力。
- 增加 SQLite/PostgreSQL 双模式。
- 增加本地/S3 兼容对象存储双模式。
- 增加进程内任务/Redis + Arq 双模式抽象。
- 增加请求日志、限流和可选 Sentry 错误追踪。
- 增加开发默认用户和旧数据归属迁移。
- 增加音频、视频静态资源路由。
- 扩充 `.env.example` 中的 TTS、ATA、TOS、数据库、对象存储、任务队列和认证配置。
- 增加 Mock 会话与 TTS 调试页面。

### 数据修复

- 清理会话 `1af8ba5e-bb49-425c-b8f8-ad5d597bad0c` 中由错误意图识别产生的脚本 v2、v3。
- 恢复脚本 v1 和会话标题“一只马蜂，干掉三吨野牛”。
- 清理基于错误脚本生成的音轨、成片记录和两条错误脚本卡片。
- 保留原始分镜和 12 张分镜图片。
- 清理前生成可恢复数据库备份 `data/app.sqlite.bak-before-script-cleanup-20260808-1af8ba5e`。

### 验证

- 后端相关模块通过 `compileall` 语法检查。
- 前端通过 Next.js 生产构建、Lint 和 TypeScript 类型检查。
- 豆包 TTS 使用当前环境完成真实短文本请求验证。
- “爽快思思”完整默认参数请求成功并返回有效 MP3 音频。
- 素材规划器验证可从当前分镜 `0/12` 回退选择历史完整分镜 `12/12`。
- 前端 `3001` 和后端 `8000` 多次健康检查返回 HTTP 200。

### 注意事项

- 已生成的旧音轨不会自动应用新的 TTS 参数，需要重新生成。
- 已生成的旧视频不会自动应用新的字幕、背景音乐或对齐算法，需要重新合成。
- 历史画面与新口播差异过大时，时间对齐无法替代重新生成分镜。
- 调试页面和开发默认账号不应直接暴露在生产环境。
- 真实密钥只能存放在未提交的 `.env` 或生产密钥管理服务中。
