以 LangGraph 作为核心编排层、LangChain 作为模型与工具适配层，构建“ChatGPT 式多会话创作助手”。
核心原则是：用户面对的是聊天，不是固定表单；系统内部才运行选题、脚本、分镜、图片等工作流。
一、产品交互形态
整体界面参考 ChatGPT：
┌──────────────┬─────────────────────────────────────┐
│ 新建会话      │  当前会话标题                        │
│              ├─────────────────────────────────────┤
│ 讨好型人格视频 │  用户：帮我做一个讨好型人格短视频    │
│ 情绪内耗选题   │                                     │
│ 抖音视频仿写   │  AI：好的，我先分析选题……            │
│              │      ✓ 选题分析完成                  │
│              │      ✓ 口播稿生成完成                │
│              │      [查看脚本] [修改] [生成分镜]     │
│              │                                     │
│              │  用户：开头再尖锐一点                 │
│              │                                     │
│              │  AI：已修改脚本……                    │
│              ├─────────────────────────────────────┤
│ 设置/用户     │  输入消息……       上传文件  发送     │
└──────────────┴─────────────────────────────────────┘
用户可以：
新建、切换、重命名、删除多个会话
在一个会话中连续修改同一作品
直接输入自然语言，不要求填写复杂表单
上传图片、文档，或粘贴抖音链接
查看实时执行状态
中途取消生成
对脚本、分镜或单张图片提出局部修改
刷新页面后继续原来的会话
从历史节点重新生成不同版本
二、技术选型
编排层：LangGraph
LangGraph 比单独使用 AgentExecutor 更适合这个项目，因为它原生适合：
多轮对话状态
thread_id 隔离不同会话
Checkpoint 持久化和断点恢复
流式输出
长时间运行的图片生成任务
人工确认与中断恢复
从指定步骤重新执行
明确控制 Agent 能调用哪些工具
每个产品会话对应一个 LangGraph thread_id。
能力层：LangChain
LangChain 用于：
DeepSeek 等聊天模型接入
Prompt 模板
Tool 定义
结构化输出
消息类型
模型切换与重试
上下文裁剪
服务端
建议继续使用 Python + FastAPI，但这次保持职责清晰：
FastAPI 只负责 HTTP、鉴权、流式事件和资源接口
LangGraph 负责对话和创作流程
Service 层负责脚本、分镜、图片等业务能力
Repository 层负责数据库
后台 Worker 负责耗时任务
数据库
开发阶段可以使用 SQLite，正式版本建议 PostgreSQL。
至少分为两类存储：
业务数据库：用户、会话、消息、作品、脚本版本、分镜和图片
LangGraph Checkpoint：保存每个 thread_id 的执行状态
不要再把所有结果塞进一张 records 表。
三、核心领域模型
建议的数据关系：
User
 └── Conversation
      ├── Message
      ├── Run
      └── Project
           ├── ScriptVersion
           ├── StoryboardVersion
           │    └── StoryboardShot
           │          └── GeneratedImage
           └── SourceMaterial
主要表：
conversations
id
user_id
title
thread_id
active_project_id
created_at
updated_at
archived_at
messages
id
conversation_id
role
content
message_type
status
metadata
created_at
message_type 可以包括：
text
tool_status
script_card
storyboard_card
image_gallery
error
runs
记录一次 Agent 执行：
id
conversation_id
status
current_node
input_message_id
started_at
finished_at
error
usage
projects
表示一个正在创作的短视频项目。一段会话可以讨论一个或多个项目。
artifact_versions
脚本、分镜都必须支持版本，而不是直接覆盖：
初版脚本
用户要求修改后的第二版
回退到第一版
基于某一版脚本重新生成分镜
四、LangGraph 状态设计
建议从一个主图开始，不要一开始做复杂多 Agent 系统。
class ChatState(MessagesState):
    user_id: str
    conversation_id: str
    project_id: str | None

    intent: str | None
    active_artifact: str | None

    source_materials: list
    topic_research: dict | None
    script: dict | None
    storyboard: list | None
    images: list

    pending_action: dict | None
    run_status: str
    error: dict | None
其中：
messages 保存本会话短期上下文
conversation_id/thread_id 隔离多会话
作品内容在业务数据库持久化
Checkpoint 只负责工作流状态
用户偏好等跨会话信息放入长期 Store
不要把完整图片、超长文档或所有历史版本直接放入 LangGraph State，只存 ID 和必要摘要。
五、主工作流
建议的第一版图：
接收用户消息
      ↓
识别意图
      ↓
加载当前项目上下文
      ↓
┌──────────────────────────────────┐
│ 普通聊天                         │
│ 创建脚本                         │
│ 修改脚本                         │
│ 分析抖音参考                     │
│ 生成/修改分镜                    │
│ 生成/重绘分镜图                  │
│ 查询项目状态                     │
└──────────────────────────────────┘
      ↓
执行对应工作节点
      ↓
保存作品版本
      ↓
组织聊天回复
      ↓
结束并等待下一条消息
初期意图可以使用结构化输出：
{
  "intent": "revise_script",
  "target": "current_script",
  "scope": "opening",
  "instruction": "开头再尖锐一点",
  "needs_confirmation": false
}
主要节点：
classify_intent
load_context
extract_source
research_topic
analyze_reference
generate_script
revise_script
generate_storyboard
revise_storyboard
generate_images
respond
handle_error
六、对话与工作流的结合
不能让 Agent 每次都把整个流程从头跑一遍。
例如：
第一次消息
帮我做一个关于讨好型人格的视频

系统执行：
研究选题 → 生成脚本 → 返回脚本卡片
第二次消息
开头再狠一点，时长控制在45秒

系统识别为 revise_script，只执行：
读取当前脚本 → 局部修改 → 保存新版本
第三次消息
可以，生成竖屏分镜

系统只执行：
读取已确认脚本 → 生成9:16分镜
第四次消息
第3镜人物换成女性，其他不要动

只修改第3镜，并让依赖该镜头的图片失效，不重新生成全部分镜。
这是新版最重要的业务能力。
七、聊天消息中的结构化卡片
AI 返回结果不应该全部是 Markdown 文本。
统一采用“消息 + Artifact 卡片”：
{
  "type": "script_card",
  "content": "脚本已经生成，共约52秒。",
  "artifact": {
    "id": "script_xxx",
    "version": 2,
    "title": "你不是善良，你只是不敢被讨厌",
    "duration": 52,
    "content": "...",
    "actions": [
      "edit",
      "regenerate",
      "confirm",
      "generate_storyboard"
    ]
  }
}
前端据此渲染：
脚本卡片
分镜列表
图片画廊
执行进度
错误重试按钮
版本切换器
八、实时响应方案
推荐使用两种通道：
SSE
用于主要聊天流：
LLM 文本 Token
节点开始
节点完成
Artifact 生成
最终结果
错误消息
SSE 实现简单，足够支撑 ChatGPT 式输出。
后台任务
图片批量生成、抖音下载和语音识别可能运行数分钟，应放入后台 Worker：
API 创建任务
Worker 执行
数据库记录状态
SSE 推送进度
切换会话后任务继续运行
用户回来仍能看到结果
第一阶段可以使用 FastAPI 后台任务；稳定后换成 Redis + Celery/Dramatiq/Arq。
九、API 设计
会话
POST   /api/conversations
GET    /api/conversations
GET    /api/conversations/{id}
PATCH  /api/conversations/{id}
DELETE /api/conversations/{id}
消息
GET  /api/conversations/{id}/messages
POST /api/conversations/{id}/messages
GET  /api/conversations/{id}/stream
POST /api/runs/{id}/cancel
作品
GET  /api/projects/{id}
GET  /api/projects/{id}/scripts
GET  /api/projects/{id}/storyboards
GET  /api/projects/{id}/images
POST /api/artifacts/{id}/activate
用户发送消息时，服务端内部使用：
config = {
    "configurable": {
        "thread_id": conversation.thread_id
    }
}
LangGraph Checkpointer 会按 thread_id 恢复对应会话状态。
十、前端规划
建议采用 React/Next.js，但前端只作为产品界面，不承载 Agent 业务逻辑。
页面结构：
/app
  /chat/[conversationId]
  /settings

/components
  /chat
    ConversationSidebar
    MessageList
    Composer
    StreamingMessage
    RunProgress
  /artifacts
    ScriptCard
    StoryboardCard
    ShotEditor
    ImageGallery
    VersionSelector
核心前端状态：
当前用户
会话列表
当前会话消息
当前运行状态
SSE 连接
本地草稿
服务端数据建议使用 TanStack Query；界面临时状态可使用 Zustand。
十一、旧版代码迁移策略
现有代码中可以保留并改造：
viral_agent/media.py：抖音下载、音频提取和 ASR
viral_agent/image_gen.py：火山方舟图片生成
viral_agent/prompts.py：提示词素材
viral_agent/storage.py：作为数据迁移参考
viral_agent/tools.py：拆成独立业务 Service
不建议继续沿用：
AgentExecutor 自动决定整个执行流程
Gradio 页面
线程 + Queue 的自定义流式机制
一个 JSON 字段保存整个作品
直接覆盖脚本和分镜
工具内部吞掉所有异常并返回普通字符串
十二、推荐目录结构
VideoGenFlow/
├── apps/
│   ├── api/
│   │   └── app/
│   │       ├── api/
│   │       ├── graph/
│   │       │   ├── state.py
│   │       │   ├── nodes/
│   │       │   ├── routes.py
│   │       │   └── builder.py
│   │       ├── services/
│   │       │   ├── script_service.py
│   │       │   ├── storyboard_service.py
│   │       │   ├── image_service.py
│   │       │   └── media_service.py
│   │       ├── repositories/
│   │       ├── models/
│   │       └── main.py
│   └── web/
├── packages/
│   └── prompts/
├── tests/
│   ├── unit/
│   ├── graph/
│   └── api/
├── migrations/
└── data/
十三、分阶段实施
第一阶段：聊天与多会话基座
新建、切换、重命名和删除会话
消息持久化
LangGraph thread_id
Checkpoint 持久化
SSE 流式回复
普通聊天和脚本生成
刷新后恢复会话
这是第一个可用版本。
第二阶段：作品化
Project 与 Conversation 分离
脚本卡片
脚本多版本
自然语言局部修改
当前版本确认和回退
第三阶段：分镜工作流
生成分镜
单镜头修改
分镜版本管理
风格和比例配置
结构化分镜卡片
第四阶段：图片与后台任务
批量生成分镜图
单张重绘
任务取消与重试
后台运行
会话间切换不影响任务
图片一致性处理
第五阶段：生产能力
用户认证
PostgreSQL
对象存储
Redis Worker
限流和用量统计
Prompt/模型版本记录
日志、监控和错误追踪
数据导出与删除
最终建议
这次不要直接做“全自动多 Agent”。第一版应采用：
单个主 LangGraph + 明确意图路由 + 专业业务节点 + 持久化多会话 + 结构化 Artifact

这样既能获得 ChatGPT 式的自然交互，又能保证脚本、分镜和图片生成过程可控制、可恢复、可测试。等单图稳定后，再考虑把选题研究、编剧、分镜导演拆成独立子图或专业 Agent。