# ============================================================
# 学习脚本依赖(01-13 示例 + 旧版 viral_agent/Gradio)
# ============================================================
langchain>=0.3.0
langchain-openai>=0.3.0
langchain-community>=0.3.0
langchain-experimental>=0.3.0
langchain-text-splitters>=0.3.0
python-dotenv>=1.0.0
scikit-learn>=1.0.0
beautifulsoup4>=4.12.0
requests>=2.28.0
gradio>=4.0.0
huggingface_hub>=0.25.0,<1.0.0
yt-dlp>=2024.10.0
faster-whisper>=1.0.3
lxml>=4.9.0

# ============================================================
# 新版 API 依赖(apps/api,FastAPI + LangGraph + SQLAlchemy)
# ============================================================
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
sqlalchemy>=2.0.0
greenlet>=3.0.0
aiosqlite>=0.19.0
sse-starlette>=2.0.0
pydantic>=2.6.0
httpx>=0.27.0

# ============================================================
# Phase 5 生产能力依赖
# ============================================================
# PostgreSQL(置 DATABASE_URL=postgresql+asyncpg://... 时启用)
asyncpg>=0.29.0
psycopg[binary]>=3.1.0
langgraph-checkpoint-postgres>=2.0.0
# 对象存储(置 STORAGE_BACKEND=s3 时启用)
aioboto3>=12.0.0
# 后台任务(置 TASK_RUNNER=arq 时启用)
arq>=0.26.0
# 用户认证
bcrypt>=4.0.0
PyJWT>=2.8.0
# 限流
slowapi>=0.1.9
# 监控/错误追踪(置 SENTRY_DSN 时启用)
sentry-sdk[fastapi]>=2.0.0
